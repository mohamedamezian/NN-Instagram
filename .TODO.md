Diagnostic steps for Invalid redirect_uri issue

- [ ] Confirm INSTAGRAM_REDIRECT_URI in `.env` matches Meta app redirect exactly
- [ ] Confirm code builds redirect_uri using `process.env.INSTAGRAM_REDIRECT_URI` (checked)
- [ ] Reproduce OAuth request and capture `redirect_uri` query param sent to Instagram
- [ ] If mismatch (trailing slash, encoding), normalize `INSTAGRAM_REDIRECT_URI` and `shopify.app.toml`
- [ ] If using dev preview (trycloudflare), add that host to allowed redirect URIs in Meta app
